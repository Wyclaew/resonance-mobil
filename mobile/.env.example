# Supabase (senkron). Masaüstündeki `src/lib/sync/config.ts` ile AYNI proje olmalı,
# yoksa cihazlar birbirini görmez.
# 🔐 anon key gizli değildir (RLS korur); service_role anahtarını ASLA koyma.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
